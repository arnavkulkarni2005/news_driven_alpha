services:
  # Backend API Service (Flask with Gunicorn)
  - type: web
    name: sentiment-api
    plan: free
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/tickers
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sentiment-db
          property: connectionString
      - key: STARTUP_COMMAND
        value: api
      - key: NEWS_API_KEY # This is the corrected line
        sync: false

  # Frontend Service (Streamlit)
  - type: web
    name: sentiment-frontend
    plan: free
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sentiment-db
          property: connectionString
      - key: STARTUP_COMMAND
        value: frontend
      - key: NEWS_API_KEY
        sync: false

  # Scheduler Worker
  - type: worker
    name: scheduler
    plan: free
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sentiment-db
          property: connectionString
      - key: STARTUP_COMMAND
        value: scheduler
      - key: NEWS_API_KEY
        sync: false

databases:
  # Free PostgreSQL Database
  - name: sentiment-db
    plan: free